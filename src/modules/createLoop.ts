// this function needs to take a loop object (form of the Loop type) and make a loop for toneJS to play

import {LoopT} from '../model'

export default function createLoop(loop: LoopT) {
  /* 
  something like for note of flatloop
  if note.active
  const addNoteLoop = new Tone.Loop(time=>{
    synthLoop.triggerAttackRelease(kick, 16n, time);
  }, 4m).start()
  
  */
}